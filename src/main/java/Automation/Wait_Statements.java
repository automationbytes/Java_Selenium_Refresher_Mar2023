package Automation;

public class Wait_Statements {

	/*
	 * Hard Wait - It will pause our execution. Even object is loaded it will wait
	 * Thread.Sleep () - its java wait
	 * Soft Wait - It will wait until the object is loaded
	 * 4 types
	 * 
	 * 1) pageloadtimeout - url
	 * 2) implicitwait - driver
	 * 3) explicitwait - single element
	 * 4) fluent wait - extn of explicit wait
	 * 
	 */
}
